{
  "name": "megabus-scraper-nodejs",
  "version": "0.0.1",
  "description": "Megabus price scraper API in Node.js",
  "scripts": {
    "start": "npm run start:dev",
    "start:dev": "NODE_ENV=development PORT=3000 npm run micro",
    "start:prod": "NODE_ENV=production npm run micro",
    "micro": "micro -p $PORT app/index.js",
    "test": "NODE_ENV=test ava test"
  },
  "author": "Daniel Ireson",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^0.22.0",
    "dotenv": "^4.0.0",
    "micro": "^6.2.0",
    "moment": "^2.17.1",
    "request": "^2.79.0",
    "request-promise": "^4.1.1"
  },
  "devDependencies": {
    "async-to-gen": "^1.3.2",
    "ava": "^0.18.1",
    "test-listen": "^1.0.1"
  }
}
